# HANDOFF — מעבר עבודה למחשב אחר

> **קובץ זמני.** נוצר 2026-08-12 כדי להעביר את העבודה הפתוחה למחשב אחר.
> **למחוק לפני המיזוג של PR #116** — הוא לא אמור להגיע ל-`main`.

---

## מה פתוח כרגע

שני PRs **ערומים זה על זה**. הסדר קריטי — התחתון ראשון.

| | PR | ענף | base | מה יש בו |
|---|---|---|---|---|
| ⬇️ תחתון | [#115](https://github.com/nimig10/machsan/pull/115) | `docs/claude-md-refresh` | `main` | רענון `CLAUDE.md` בלבד. אפס שינויי קוד |
| ⬆️ עליון | [#116](https://github.com/nimig10/machsan/pull/116) | `feat/reservation-whatsapp` | `docs/claude-md-refresh` | לחצן וואטסאפ + תיקון בעיית הטלפונים |

**שלושה קומיטים סה"כ:**

```
22133d2  fix(phone): מספר שמולא אוטומטית דרס מספר שהסטודנט הקליד   ← #116
e086db8  feat(reservations): לחצן וואטסאפ לסטודנט + סגירת מקור בעיית הטלפונים  ← #116
f620779  docs(claude): רענון מספרים שהתיישנו + לקח #50            ← #115
f64411c  (main) Merge pull request #114                            ← נקודת המוצא
```

---

## איך מורידים את זה במחשב החדש

```bash
git clone https://github.com/nimig10/machsan.git
cd machsan
npm ci

# שני הענפים כבר ב-origin — פשוט למשוך אותם
git fetch origin
git checkout feat/reservation-whatsapp
```

`feat/reservation-whatsapp` הוא **האיחוד של הכל** (הוא ערום מעל ענף התיעוד), ולכן
זה הענף היחיד שצריך להריץ על `localhost` — לפי חוק מרכוז ה-localhost ב-`CLAUDE.md`.
אין צורך לעשות checkout ל-`docs/claude-md-refresh` בנפרד.

### מה צריך להביא ידנית — `.env.local`

**הקובץ הזה לא בגיט ולא יגיע עם ה-clone.** בלעדיו האפליקציה לא תעלה.
להעתיק אותו מהמחשב הנוכחי מ-`c:\machsan\.env.local`.

הוא מכיל את המפתחות של **dev** (`mhvujejdlmtowypjdhjd`) — לוודא שזה מה שמועתק
ולא של פרוד.

### להרים

```bash
npm run dev     # http://localhost:5174  (הפורט נעול ב-vite.config.js)
```

⚠️ אם השרת לא עולה — כנראה תהליך `node` ישן תפוס על 5174. `strictPort` נופל בשקט.

`/api/*` רץ מקומית in-process דרך `scripts/vite-api-plugin.mjs`, כולל cache-bust
בכל בקשה — אז אפשר לבדוק endpoints בלי `vercel dev` ובלי Preview. אימות מהיר:
`GET /api/<route>` מחזיר 401/400 ולא 404.

---

## איפה זה עמד כשעצרנו

**הקוד גמור ועובר את כל השערים. מה שחסר זו הבדיקה הידנית בדפדפן.**

- `lint` 0 errors (216 warnings — זהה ל-`main`, לא נוספו חדשות)
- `build` ✓
- כל חבילות הבדיקה ירוקות:
  `phone 39/39` · `prodvis 22/22` · `policy 27/27` · `dates 54/54` · `return 78/78` ·
  `auth 12/12` · `announce 32/32` · `night 67/67` · `ics 20/20`
- `test:db` לא הורץ (דורש מפתחות dev; רץ ב-CI כשהם מוגדרים — כרגע מדלג נקי)

### ⚠️ ה-dev DB מזוהם מהבדיקה הקודמת

הרובריקה של **שלומי ברכה** (`nimig10+s3@gmail.com`) מחזיקה את המספר **הישן**
`0535250826`, שנכתב שם על ידי הגרסה הבאגית. **למחוק אותו ידנית מאדמיניסטרציה ›
סטודנטים לפני שחוזרים על התרחיש**, אחרת הבדיקה לא אומרת כלום.

---

## מה צריך לבדוק (Stage 1 — localhost על dev)

### התרחיש שנכשל פעם אחת — הכי חשוב
1. למחוק את הטלפון של סטודנט מהרובריקה
2. הפקה חדשה + "טלפון הבמאי" = מספר חדש → **הרובריקה מתעדכנת כבר בשמירה**,
   בלי להגיש ציוד
3. להגיש רשימת ציוד → הבקשה נושאת את המספר **החדש**, לא את הישן

### דריסה — האנטי-רגרסיה המרכזית
לחזור על 4–5 עבור **כל אחד** מ-`פרטית` · `סאונד` · `קולנוע יומית`, **בתאריכים
שונים** — אותו סטודנט לא יכול להגיש שתי בקשות חופפות, וה-guard יחזיר
`student_overlap` שנראה כמו תקלה אבל אינו כזה.

4. סטודנט עם מספר מגיש בקשה **בלי לגעת** בשדה הטלפון → הרובריקה **לא זזה**
5. אותו סטודנט מקליד מספר אחר בשדה → **מתעדכן**
6. "הגדרות חשבון": לשנות **רק את השם** ולשמור → **הטלפון שורד**
7. שם: למחוק את השדה במפורש ולשמור → נמחק (מחיקה מכוונת עדיין עובדת)

### הלחצן
8. לוח הבקרה → בקשה עם טלפון → לחצן ירוק פותח וואטסאפ עם הנוסח הנכון
9. שעות **בלי שניות** בהודעה (`15:30` ולא `15:30:00`)
10. בקשה `הוחזר`/`בוטל` → וואטסאפ מופיע, "עריכת בקשה" **לא**
11. סטודנט בלי טלפון בשום מקור → טקסט אפור, בלי קישור מת
12. רוחב צר / PWA → השורה נשברת ולא מייצרת גלילה אופקית

### הפקות קיימות — הדרישה המפורשת שלא ייפגעו
13. לפתוח, לא לגעת בכלום, לסגור ב-X → **שום שינוי**
14. להקליד טלפון ולסגור ב-**X** (לא "שמור") → **נשמר** (לקח #46)

---

## סדר המיזוג כשהבדיקה עוברת

1. למזג **#115 קודם** (התחתון)
2. אחרי מיזוגו, ה-base של #116 יתעדכן ל-`main` אוטומטית. אם GitHub לא עשה זאת —
   `git checkout feat/reservation-whatsapp && git rebase main && git push -f`
3. **למחוק את `HANDOFF.md`** (הקובץ הזה) לפני מיזוג #116
4. למזג #116

**אין מיגרציה בשום שלב** — הכל code-only. `productions.director_phone` כבר קיים
מ-`20260515110000`. אין מה להריץ על פרוד לפני המיזוג.

### Stage 4 — אחרי המיזוג
לכתוב לארכיון (Obsidian vault + Google Drive): שורה ב-`03-ציר-זמן/2026-08.md`
לשני ה-PRs, ונוט ב-`01-מפת-המערכת/` על מקורות הטלפון של הסטודנט — הנושא הזה
נשבר פה פעמיים ביום אחד ומגיע לו תיעוד עומק.

---

## רקע: למה ה-PR הזה גדול ממה שהתחיל

התחיל כבקשה ללחצן וואטסאפ בפאנל הבקשה. תוך כדי התברר שאין למי להתקשר:
**21 מתוך 174 סטודנטים** (12%) עם טלפון ברובריקה, ו-**25 מתוך 31 בקשות הפקה**
בלי טלפון בכלל.

שני שורשים בקוד: טופס ההפקה מעולם לא ביקש טלפון, ומה שהסטודנט כן הקליד נשמר
כסנאפ-שוט על שורת הבקשה ולא חזר לרובריקה.

הבדיקה הידנית תפסה באג בגרסה הראשונה של התיקון (מספר שמולא-אוטומטית דרס מספר
שהוקלד), והוא תוקן בקומיט `22133d2`. **העיקרון שיצא מזה, ושווה לזכור:**

> ערך שמולא אוטומטית אינו קלט משתמש, ואסור לו לדרוס נתון קיים.

בדרך נתפס גם באג ותיק ולא-קשור: פאנל "הגדרות חשבון" **מחק טלפונים** — סטודנט
שפתח אותו כדי לשנות רק את שמו מחק בכך את הטלפון של עצמו. תוקן גם הוא כאן.

---

## תיעוד מלא

- תוכנית העבודה: `C:\Users\NIMROD\.claude\plans\warm-juggling-meadow.md` (מקומי,
  לא בגיט — לא יגיע למחשב החדש; התוכן המהותי נמצא בתיאורי שני ה-PRs)
- ההקשר הקבוע של הפרויקט: `CLAUDE.md` בשורש
